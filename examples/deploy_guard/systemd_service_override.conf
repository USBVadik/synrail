[Service]
# Example only:
# - use a drop-in override to gate every restart through Synrail
# - replace these placeholder paths with your real install locations
Environment=SYNRAIL_ARTIFACT_ROOT=/srv/example-app/.synrail
ExecStartPre=/opt/synrail/tools/reference/synrail_deploy_guard.sh --artifact-root /srv/example-app/.synrail
