{
  "type": "object",
  "required": [
    "schema_version",
    "run_id",
    "task_class",
    "current_step_id",
    "allowed_scope",
    "forbidden_scope",
    "must_pass",
    "prompt"
  ],
  "properties": {
    "schema_version": {"type": "string", "const": "repair_prompt_bridge_record_v0"},
    "run_id": {"type": "string", "minLength": 1},
    "task_class": {"type": "string", "minLength": 1},
    "current_step_id": {"type": "string"},
    "allowed_scope": {"type": "array", "items": {"type": "string", "minLength": 1}},
    "forbidden_scope": {"type": "array", "items": {"type": "string", "minLength": 1}},
    "must_pass": {"type": "array", "items": {"type": "string", "minLength": 1}},
    "prompt": {"type": "string", "minLength": 1}
  },
  "additionalProperties": false
}
