{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "record_count",
    "labels",
    "total_line_reduction",
    "truth_preserved_all",
    "render_shorter_all",
    "verdict",
    "why"
  ],
  "properties": {
    "schema_version": {"const": "operator_render_adoption_delta_v0"},
    "record_count": {"type": "integer", "minimum": 1},
    "labels": {"type": "array", "items": {"type": "string", "minLength": 1}},
    "total_line_reduction": {"type": "integer"},
    "truth_preserved_all": {"type": "boolean"},
    "render_shorter_all": {"type": "boolean"},
    "verdict": {"type": "string", "enum": ["READING_TAX_REDUCED_WITHOUT_TRUTH_LOSS", "MIXED"]},
    "why": {"type": "string", "minLength": 1}
  }
}
