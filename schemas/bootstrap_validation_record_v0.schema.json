{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "run_id",
    "task_class",
    "status",
    "reason",
    "controlled_mode",
    "started_via",
    "run_id_matches",
    "task_class_matches",
    "task_identity_present",
    "prompt_identity_present",
    "project_root_matches",
    "artifact_root_matches",
    "target_path_matches",
    "target_classification_matches",
    "target_identity_matches",
    "baseline_identity_matches",
    "execution_surface_identity_matches",
    "intended_run_class_matches"
  ],
  "properties": {
    "schema_version": {"const": "bootstrap_validation_record_v0"},
    "run_id": {"type": "string", "minLength": 1},
    "task_class": {"type": "string", "minLength": 1},
    "status": {"type": "string", "enum": ["VALID", "INVALID"]},
    "reason": {"type": "string", "minLength": 1},
    "controlled_mode": {"type": "boolean"},
    "started_via": {"type": "string"},
    "run_id_matches": {"type": "boolean"},
    "task_class_matches": {"type": "boolean"},
    "task_identity_present": {"type": "boolean"},
    "prompt_identity_present": {"type": "boolean"},
    "project_root_matches": {"type": "boolean"},
    "artifact_root_matches": {"type": "boolean"},
    "target_path_matches": {"type": "boolean"},
    "target_classification_matches": {"type": "boolean"},
    "target_identity_matches": {"type": "boolean"},
    "baseline_identity_matches": {"type": "boolean"},
    "execution_surface_identity_matches": {"type": "boolean"},
    "intended_run_class_matches": {"type": "boolean"}
  }
}
