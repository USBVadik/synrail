{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "run_id",
    "task_class",
    "primary_action",
    "allowed_scope",
    "forbidden_scope",
    "must_pass",
    "prompt"
  ],
  "properties": {
    "schema_version": {"type": "string", "const": "consistency_recovery_prompt_record_v0"},
    "run_id": {"type": "string"},
    "task_class": {"type": "string"},
    "primary_action": {"type": "string"},
    "allowed_scope": {"type": "array", "items": {"type": "string"}},
    "forbidden_scope": {"type": "array", "items": {"type": "string"}},
    "must_pass": {"type": "array", "items": {"type": "string"}},
    "prompt": {"type": "string"}
  }
}
