# Baseline Comparison Candidate 002

## Purpose

Select a second comparison scenario that is less favorable to `Synrail` than the first killer-path record.

This document exists so the product is not judged only on its strongest case.

## Selected candidate

The second comparison candidate is:

- a narrow honesty-restoration incident where the main fix is a truthful fallback rather than a full closure-grade exact-task success

Concrete reference shape:

- a downstream capability was overclaimed
- the system needed to stop pretending it could do the thing reliably
- the practical repair was a narrow guard or fallback path

## Why this candidate is less favorable

This candidate is less favorable to `Synrail` because:

- the bounded fix is simpler
- the product risk is narrower
- a disciplined lightweight operator loop could plausibly handle it competently
- the overhead of the full `Synrail` path is more exposed

## Why it still matters

This candidate matters because it tests whether:

- `Synrail` still adds value when the scenario is not maximally hostile
- or whether the simpler baseline is already good enough here

That is exactly the kind of pressure needed to avoid overclaiming the wedge.

## What the comparison should ask

The comparison should ask:

- in a small honesty-restoration incident, does `Synrail` still reduce meaningful risk enough to justify its extra coordination cost?

## Likely comparison tension

Expected tension on this candidate:

- `Synrail` will still improve explicit truth discipline
- but the simpler baseline may already be “good enough” at lower cost

That makes this candidate valuable even if the result is weaker for `Synrail`.

## Next move

Use this candidate to produce:

- one second compact comparison record

That record should be allowed to conclude:

- `Synrail better`
- `baseline good enough`
- `unclear`

without pressure to make `Synrail` win.
