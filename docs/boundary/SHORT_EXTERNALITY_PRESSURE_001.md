# SHORT_EXTERNALITY_PRESSURE_001

## Purpose

Record the first short external-ish pressure pass on the tightened kernel.

This slice exists to answer one bounded question before the next harsher substitute tranche:

- does one uglier repeated-doctor contour still hold together when we read it through three different external-ish pressure dimensions?

## Artifacts

The canonical slice now lives at:

- `tools/reference/synrail_externality_pressure_v0.py`
- `schemas/externality_pressure_record_v0.schema.json`
- `fixtures/externality_pressure_001/pressure.json`

Input records:

- `fixtures/reproducibility_test_002/reproducibility.json`
- `fixtures/second_operator_test_002/second_operator.json`
- `fixtures/operator_reading_test_001/operator_reading.json`

## What it measures

This short pass does not invent a new product surface.

It only asks whether one uglier contour simultaneously survives:

1. key-truth reproducibility
2. packet-first second-operator followability
3. human-readable render followability

## Current honest result

The current pressure record says:

- `contour_reason = DOCTOR_NOT_GREEN`
- `packet_only_entry = true`
- `requires_author_intuition = false`
- `dimensions_passed = [REPRODUCIBILITY, SECOND_OPERATOR, OPERATOR_READING]`
- `uncovered_risks = []`
- `verdict = SURVIVES_SHORT_EXTERNAL_PRESSURE`

## Why this matters

This is the right bridge between:

- internal kernel tightening
- and the next harsher substitute comparison tranche

because it checks whether the same uglier contour is still holding together across multiple external-ish readings, not only inside one canonical artifact grammar.

## Current reading

The shortest honest reading is:

- one uglier repeated-doctor contour now survives the first short external-ish pressure pass
- that does not prove broad external necessity yet
- but it does mean the tightened kernel is stable enough to deserve the next harsher substitute test
