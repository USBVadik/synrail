# SUBSTITUTE_KILL_TEST_005

## Purpose

Pressure-test the tightened kernel against one more harsher substitute tranche on a different ugly family after the first external-ish repeated-doctor win.

This slice asks a stricter follow-up question:

- does the tightened kernel still win when the substitute comparison moves away from the repeated-doctor contour and instead pressures:
  - one fresh-orchestration non-resumable boundary
  - one selected prepared multi-step repair chain

## Artifacts

The fifth substitute-pressure slice now lives at:

- `fixtures/substitute_kill_test_005/record_fresh_orchestration.json`
- `fixtures/substitute_kill_test_005/record_selection_chain.json`
- `fixtures/substitute_kill_test_005/record_weak.json`
- `fixtures/substitute_kill_test_005/substitute_pressure.json`

## Substitute stacks tested

1. `manual_forward_orchestration_runbook`
2. `manual_stage_runbook_chain`
3. `bounded_patch_validation_loop`

## Current honest result

The current pressure record says:

- `SYNRAIL_BETTER = 2`
- `SUBSTITUTE_GOOD_ENOUGH = 0`
- `UNCLEAR = 1`

Winning wedges:

- `SUBSTITUTE_SCENARIO_FRESH_ORCHESTRATION_005`
  - strongest edge: one fresh-orchestration non-resumable contour now blocks the wrong named `resume` path immediately and points the operator back to the governed forward contour without hidden judgment
- `SUBSTITUTE_SCENARIO_SELECTION_CHAIN_005`
  - strongest edge: one selected prepared multi-step contour now carries repair order, packet-first continuation, and truthful acceptance history without forcing the operator to rebuild the chain from a manual runbook

Still non-decisive:

- `SUBSTITUTE_SCENARIO_WEAK_005`
  - the lightweight bounded patch loop still remains plausible on the weak path

## Why this matters

This slice is the right next step after the previous external-ish repeated-doctor tranche.

It shows that the tightened kernel is not only stronger on one repeated pressure family.

It also keeps an edge on:

- one non-resumable forward-orchestration boundary
- one selected prepared repair chain with multiple ordered steps

without pretending that every lower-risk path should now belong to Synrail.

## Current reading

The shortest honest reading is:

- Synrail now has one stronger substitute edge on a fresh-orchestration non-resumable boundary
- it also keeps an edge on one selected prepared multi-step repair chain
- the weak path still stays non-decisive
- and this tranche still did not reveal a new core bug that justified immediate kernel surgery
