# SUBSTITUTE_KILL_TEST_003

## Purpose

Pressure-test the tightened kernel against harsher substitute stacks that sit closer to ugly continuation reality.

This slice no longer leans on the cleaner strong wedge or the first minimal second-operator contour.

Instead it asks:

- does the tightened kernel still win when repeated doctor pressure and less-curated packet-first continuation are the comparison target?

## Artifacts

The third substitute-pressure slice now lives at:

- `fixtures/substitute_kill_test_003/record_doctor_return.json`
- `fixtures/substitute_kill_test_003/record_second_operator.json`
- `fixtures/substitute_kill_test_003/record_weak.json`
- `fixtures/substitute_kill_test_003/substitute_pressure.json`

## Substitute stacks tested

1. `manual_identity_repair_checklist`
2. `stage_runbook_resume_checklist`
3. `bounded_patch_validation_loop`

## Current honest result

The current pressure record says:

- `SYNRAIL_BETTER = 2`
- `SUBSTITUTE_GOOD_ENOUGH = 0`
- `UNCLEAR = 1`

Winning harsher wedges:

- `SUBSTITUTE_SCENARIO_DOCTOR_RETURN_003`
  - strongest edge: repeated doctor-pressure control with bounded repair history and explicit stop conditions
- `SUBSTITUTE_SCENARIO_SECOND_OPERATOR_003`
  - packet-first less-curated continuation with explicit next-step truth and lower author-memory dependence

Still non-decisive:

- `SUBSTITUTE_SCENARIO_WEAK_003`
  - the lightweight bounded patch loop still remains plausible on the weak path

## Why this matters

This slice is stricter than the last one because it pressures the kernel on uglier continuation contours instead of only on cleaner convergence and minimal packet-entry examples.

The useful signal is:

- the tightened kernel now also wins on one repeated-doctor ugly contour
- and on one less-curated stage-level continuation contour
- but it still does not universalize into every low-risk path

## Current reading

The shortest honest reading is:

- `Synrail` now has a harsher substitute edge on repeated doctor-return continuation
- it also keeps a harsher edge on less-curated packet-first second-operator continuation
- the weak path still refuses to become a fake universal victory story
