# SUBSTITUTE_KILL_TEST_004

## Purpose

Pressure-test the tightened kernel against one more external-ish substitute tranche after the short externality bridge.

This slice asks a slightly stricter question than the previous one:

- does the tightened kernel still win when the substitute comparison is anchored in the same uglier repeated-doctor contour that already survived reproducibility, second-operator, and operator-reading pressure?

## Artifacts

The fourth substitute-pressure slice now lives at:

- `fixtures/substitute_kill_test_004/record_external_doctor.json`
- `fixtures/substitute_kill_test_004/record_recovery_repair.json`
- `fixtures/substitute_kill_test_004/record_weak.json`
- `fixtures/substitute_kill_test_004/substitute_pressure.json`

## Substitute stacks tested

1. `manual_resume_triage_sheet`
2. `manual_recovery_reverification_checklist`
3. `bounded_patch_validation_loop`

## Current honest result

The current pressure record says:

- `SYNRAIL_BETTER = 2`
- `SUBSTITUTE_GOOD_ENOUGH = 0`
- `UNCLEAR = 1`

Winning wedges:

- `SUBSTITUTE_SCENARIO_EXTERNAL_DOCTOR_004`
  - strongest edge: one uglier repeated-doctor contour now survives short external-ish pressure with bounded history, packet-only entry, and explicit stop semantics
- `SUBSTITUTE_SCENARIO_RECOVERY_REPAIR_004`
  - strongest edge: the repairable recovery contour keeps the current step, required inputs, and stale sub-surfaces explicit without raising recovery cost above the substitute path

Still non-decisive:

- `SUBSTITUTE_SCENARIO_WEAK_004`
  - the lightweight bounded patch loop still remains plausible on the weak path

## Why this matters

This slice is the right next step after the short externality bridge.

It shows that the tighter kernel is not only internally coherent on that uglier contour.

It also still wins against a simpler manual substitute on:

- repeated doctor-pressure control
- repairable recovery-step clarity

without pretending that every weak path now belongs to Synrail.

## Current reading

The shortest honest reading is:

- Synrail now has one stronger substitute edge on an external-ish repeated-doctor contour
- it also keeps an edge on one repairable recovery contour
- the weak path still stays non-decisive
- and this tranche did not reveal a new core bug that justified immediate kernel surgery
